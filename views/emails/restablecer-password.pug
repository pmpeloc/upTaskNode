<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css    
    body
        h2 Reestablecer Password
        p Hola, has solicitado restablecer tu password, haz click en el siguiente enlace para restablecerlo, este enlace es temporal, asi que si se vence, vuelve a solicitarlo
        a(href=`${resetUrl}`) Ir a Resetear el Password
        p Si no puedes acceder a este enlace, vistia #{resetUrl}
        p Si no solicitaste este email, puedes ignorarlo