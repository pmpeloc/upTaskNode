<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css    
    body
        h2 Confirma tu cuenta UpTask
        p Ya estas casi inscripto, solo preciona el siguiente botón
        a(href=`${confirmarUrl}`) Confirmar Cuenta
        p Si no puedes acceder a este enlace, vistia #{confirmarUrl}